const tablaMultiplicar = (numero) => {
    for (let i = 1; i <= 10; i++) {
      console.log(`${numero} x ${i} = ${numero * i}`);
    }
  };
  
  // Ejemplo de uso:
  const numero = 7;
  tablaMultiplicar(numero);
  