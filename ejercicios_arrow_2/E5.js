const decimalToBinary = (numero) => {
    return numero.toString(2);
  };
  
  // Ejemplo de uso:
  const numeroDecimal = 10;
  const numeroBinario = decimalToBinary(numeroDecimal);
  console.log(`${numeroDecimal} en binario es ${numeroBinario}`);
  